# 06 — 수정 명령문 (실행 모델용)

> 소비자: **Sonnet 5 빌더 세션** (검수: Opus 4.8, `08-inspection-log.md`에 판정 기록)
> 근거: `05-audit-report.md`의 발견 F-01~F-15 · 실행 순서와 게이트는 `02-master-plan.md` 참조
> 실행 배치(02 확정): FIX-07·06→P1-S1·S2(G1 필수) / V-01→FIX-01→P1-S3 / FIX-15→P1-S4 / FIX-08·09→P1-S5 / FIX-11·12→P1-S6 / V-02~05·FIX-13→P1-S7 / FIX-10→P3-S2 / FIX-04·05(2단계)→사용자 go/no-go 후(02 §3)
> **공통 규칙 (모든 명령에 우선):**
> 1. **삭제 금지.** 이 문서에는 rm·파일 삭제·Gist 파일 제거 명령이 없다. 대체된 옛 파일은 §정리 후보에 집계만 하고 사용자 승인을 기다린다.
> 2. **백업 선행.** 기존 파일 수정 전 반드시 `<파일명>.bak-YYYYMMDD` 사본 생성(이미 있으면 덮어쓰지 말고 `-2` 붙임). 같은 파일 수정 2회 연속 실패 시 중단·보고(전역 CLAUDE.md 규칙).
> 3. **멱등.** 모든 명령은 재실행해도 안전하게 작성되어 있다(append 전 존재 확인, mkdir -p 등). 재실행 시 백업본을 다시 만들지 않는다.
> 4. **디바이스 태그**: `[REMOTE-OK]` 원격 Claude Code에서 가능 / `[IMAC-ONLY]` nurisimac Cowork 세션 필요 / `[ANY]` 어느 쪽이든 / `[USER-MANUAL]` 사용자 손이 필요한 단계 포함.
> 5. 실행 후 결과(성공/실패/증거)를 `07-execution-ledger.md`에 기록한다.

---

## A. 수리 명령 (FIX 시리즈)

### FIX-07 — 자율권 우선순위 규칙 명문화 ⚡G1 선행 (가장 먼저 실행)
- **대상**: F-07 · **태그**: `[IMAC-ONLY]` (전역 CLAUDE.md가 Cowork 동기화 폴더에 있음) + `[REMOTE-OK]` (repo CLAUDE.md 쪽)
- **목적**: 무인 스킬과 전역 "반드시 질문" 원칙의 충돌 해소. 빌더 자신의 자율권 근거.
- **백업**: Cowork GD의 `CLAUDE.md` → `CLAUDE.md.bak-<날짜>`
- **명령**: 전역 CLAUDE.md 말미에 다음 섹션을 추가한다(이미 "# 지시 우선순위" 섹션이 있으면 건너뜀):

```markdown
# 지시 우선순위 (2026-07 정례화)
1. 안전 규칙은 항상 최우선: 파일 무단 삭제 금지, 수정 전 백업, 같은 파일 2회 연속 수정 실패 시 중단·보고.
2. "무인/자율 실행"이 명시된 스킬·작업 지시문은, 그 작업 범위 안에서 "불명확하면 질문" 원칙에 우선한다.
3. 무인 실행 중 불명확한 사항이 생기면: 질문으로 멈추지 말고 (a) 보수적 기본값 선택, (b) 선택 내용을 작업 로그에 기록, (c) 다음 보고에서 사용자에게 알린다.
4. 무인 실행 중에도 다음은 예외 없이 보류(사용자 실행 전환): 커밋 서명 변경, 금전, 삭제, 외부 발송(메일·메시지), 새 외부 서비스 가입.
```
- **검증**: 파일에 "# 지시 우선순위" 섹션 존재 + 4개 항목 확인. 무인 스킬(lm-batch) 설명과 모순 재검토.
- **롤백**: `.bak` 복원.

### FIX-06 — repo CLAUDE.md 신설 (디스패처·신규 세션 규약)
- **대상**: F-06, F-03, F-04, F-13 일부 · **태그**: `[REMOTE-OK]`
- **목적**: 코드 주석에 분산된 규약을 한 파일로. 신규 세션 온보딩 비용 제거.
- **백업**: 불필요(신규 파일). 이미 존재하면 백업 후 병합.
- **명령**: repo 루트에 `CLAUDE.md` 생성. 반드시 포함할 내용:
  1. **시스템 지도**: 대시보드 3장 역할, Gist 3종 용도(ID는 쓰지 않음 — "Dropbox의 AI Company/gist-config.md 참조"), 디스패처(=로컬 폴러, iMac 상주)와 정기 회차 04/18/23 KST.
  2. **표준 용어**: "디스패처(로컬 폴러)"로 통일 — 오케스트레이터·폴러 등 기존 표현은 동의어로 명기(F-03 해소).
  3. **타임스탬프 규약**(F-04): 저장은 KST `+09:00` 오프셋 ISO, 비교는 반드시 epoch 환산, ISO 문자열 직접 비교·정렬 금지, 과거 UTC(Z) 기록 혼재 사실 명기.
  4. **안전 보류 기준**: 커밋·금전·삭제·외부 발송은 자동 실행 금지 → "사용자 실행"+사유(기존 관례 성문화).
  5. **화이트리스트 액션 ↔ 실행 스킬 매핑표**: FIX-01 완료 후 채움(그 전까지 "TBD — FIX-01" 표기).
  6. **현행 컨텍스트**: 사용자 3역할, 사역지는 **희성교회 청소년부**(응암교회 자료는 前사역지 아카이브), 제품 Hubrary·VoiceReading.
  7. **민감정보 규칙**: Gist ID·토큰·개인정보(실명·연락처·주민번호)를 repo·공개 문서에 쓰지 않음(F-05 예방).
- **검증**: 파일 존재 + 위 7요소 전부 포함(체크리스트 대조). 다른 세션에서 repo만 열고 시스템 구조를 설명할 수 있는지 자기 시험.
- **롤백**: 파일 제거가 아니라 git revert (커밋 단위).

### FIX-01 — 실행 엔진 역문서화 (ENGINE.md)
- **대상**: F-01 · **태그**: `[IMAC-ONLY]` (실물 확인 V-01 선행) → 문서 커밋은 `[ANY]`
- **목적**: 버스 팩터 1 해소 — iMac이 사라져도 엔진을 재구축할 수 있는 명세.
- **선행**: `V-01` (엔진 실물 확인)
- **명령**: `docs/ops/ENGINE.md` 생성, V-01 결과를 기입:
  - 폴러 구현체(상주 세션 지시문 전문/launchd plist/cron 라인 — 발견된 그대로 사본), 1분 폴링 대상과 판정 로직, 정기 회차 트리거 방식(04/18/23 KST가 무엇으로 걸려 있는지), 액션 ID→실행 스킬/명령 매핑표(`research.batch`→lm-batch 추정 확인 포함), worklog 기록 형식, 실패 시 재시도 정책(있다면), **재구축 절차**(새 머신에서 엔진 재현 단계).
  - 상주 세션 지시문 원본 파일이 발견되면 `ops/`에 사본 커밋(민감정보 마스킹).
- **검증**: "이 문서만 보고 새 맥에서 엔진 재현 가능한가?" 루브릭 자기 대조. FIX-06의 매핑표 TBD 해소.
- **롤백**: git revert.

### FIX-04 — 타임스탬프 데이터 정규화 (선택, 백업 전제)
- **대상**: F-04 잔여분 · **태그**: `[IMAC-ONLY]` `[USER-MANUAL]`(go/no-go)
- **목적**: Gist 안 과거 UTC(Z) 기록을 +09:00로 일괄 환산(코드는 이미 안전하므로 **선택 과제**).
- **선행**: V-04로 혼재 규모 파악. 규모가 작으면 실행 가치 낮음 — 사용자에게 go/no-go 질문.
- **백업**: 실행 전 Gist 3종 JSON 전문을 `Drive: Claude Cowork GD/backup/gist-<이름>-<날짜>.json`으로 저장. **백업 확인 전 정규화 금지.**
- **명령**(go 시): 각 JSON의 모든 ISO 타임스탬프를 epoch 동치인 `+09:00` 표기로 재작성하는 스크립트를 작성·드라이런(변경 예정 목록 출력)→사용자 확인→적용. 정기 회차 시간대(03:50~04:20, 17:50~18:20, 22:50~23:20 KST)는 피해서 실행(R6 쓰기 충돌).
- **검증**: 적용 후 재다운로드→`Z` 문자열 0건+epoch 값 불변 확인. 대시보드 표시 정상.
- **롤백**: 백업 JSON을 Gist에 재업로드.

### FIX-05 — Gist 노출 대응
- **대상**: F-05 · **태그**: 규칙은 `[ANY]`, 로테이션은 `[IMAC-ONLY]` `[USER-MANUAL]`(go/no-go)
- **1단계(즉시, 무해)**: FIX-06의 민감정보 규칙 명문화 + V-04에서 현재 Gist 내용에 개인정보(실명·연락처)가 이미 있는지 점검. 있으면 해당 항목만 익명화 수정(백업 선행).
- **2단계(사용자 go/no-go)**: Gist ID 로테이션 — 새 비밀 Gist 3종 생성→내용 이전→대시보드 상수 3곳(`team-room.html:163-165`, `ministry-room.html:257-259`) 교체 커밋→gist-config.md(Dropbox) 갱신→각 디바이스 브라우저 localStorage 재설정 안내. **구 Gist는 삭제하지 않고** §정리 후보에 등재.
- **검증**: 새 ID로 대시보드 정상 동작(읽기/쓰기 각 1회), 구 ID는 코드에서 0건 grep.
- **롤백**: 상수 커밋 revert + localStorage 원복(구 Gist 무손상이므로 완전 롤백 가능).

### FIX-08 · FIX-09 — 경로 표준화와 디바이스 프로파일
- **대상**: F-08, F-09 · **태그**: `[IMAC-ONLY]`
- **목적**: 멀티디바이스 이식성. 표기 혼용 제거.
- **백업**: `settings.local.json` → `.bak-<날짜>` (Cowork GD `.claude/` 안).
- **명령**:
  1. **표준 경로 확정**: Dropbox는 macOS 실경로 `~/Library/CloudStorage/Dropbox` 1종으로 통일하되 permission 규칙에는 절대경로가 필요하므로, **디바이스 프로파일 방식** 채택 — repo `devices/nurisimac/settings.local.json`(현행 정리본), `devices/README.md`(새 디바이스 추가 절차: 사용자명 치환 규칙 `[USERNAME]` 플레이스홀더 → sed 1줄).
  2. settings.local.json 안 3종 표기를 실경로 1종으로 재작성(기능 동일성: 각 규칙이 가리키는 실제 폴더가 같은지 ls로 확인).
  3. 전역 CLAUDE.md·스킬 본문(FIX-15 이후)의 경로 표기도 동일 기준 일괄 점검 — 변경 목록만 만들어 `07`에 기록 후 일괄 적용.
- **검증**: 수정 후 Cowork 세션 재시작→기존 허용 동작(Dropbox 읽기, Church event mkdir 등)이 승인 프롬프트 없이 통과하는지 실사용 1회.
- **롤백**: `.bak` 복원.

### FIX-10 — 쌍둥이 스킬 단일 소스화
- **대상**: F-10 · **태그**: `[IMAC-ONLY]` `[USER-MANUAL]`(claude.ai 재업로드)
- **선행**: FIX-15(스킬 원본 repo 편입) 완료.
- **명령**: `skills/notebooklm-reports/` 아래 공통 코어(보고서 생성 프롬프트·메모 저장 로직)를 `_core.md`로 추출, 두 변형은 [환경 게이트 + 코어 include 지시]만 남긴 얇은 래퍼로 재작성. "본 파일은 _core.md에서 생성됨 — 직접 수정 금지" 헤더 삽입. skill-creator 스킬로 두 변형 재업로드(사용자 확인 단계).
- **검증**: 두 변형의 diff가 게이트 섹션만 다름을 확인. Cowork에서 notebooklm-reports 1회 기능 시험(Chrome 연결 상태에서).
- **롤백**: repo 이전 커밋의 스킬 본문으로 재업로드.

### FIX-11 — 상태 미러(하트비트)로 관측성 확보
- **대상**: F-11, F-12 감지 측면 · **태그**: `[IMAC-ONLY]` (디스패처 지시문 수정)
- **목적**: 원격 세션·타 디바이스가 시스템 생사·적체를 볼 수 있게.
- **명령**: 디스패처 지시문(V-01에서 확보)에 회차 종료 루틴 추가 — `status/heartbeat.md`를 repo에 커밋·push: 회차 시각(KST), 처리/보류/실패 건수, 실패 요약 1줄씩. **민감정보·본문 미포함**(건수와 ID만). push 실패 시 Drive `Claude Cowork GD/status/`에 대체 기록.
- **검증**: 다음 정기 회차 후 원격 세션에서 `git pull` → heartbeat 갱신 확인(이것이 F-11 해소의 실증).
- **롤백**: 지시문에서 해당 루틴 제거(파일은 잔존해도 무해).

### FIX-12 — 재기동 catch-up 규약
- **대상**: F-12 복구 측면 · **태그**: `[IMAC-ONLY]`
- **명령**: 디스패처 지시문에 추가 — "기동 시 heartbeat의 마지막 회차 시각을 확인, 놓친 정기 회차가 있으면 queued 요청을 즉시 1회 소화(catch-up 회차)하고 heartbeat에 'catch-up' 표기".
- **검증**: 의도적으로 1회차 건너뛴 뒤 재기동→catch-up 실행 확인(사용자 참관 1회).
- **롤백**: 지시문 원복(.bak).

### FIX-13 — 낡은 컨텍스트 아카이브 표기
- **대상**: F-13 · **태그**: `[REMOTE-OK]`(Notion MCP) `[USER-MANUAL]`(대량 이동 승인)
- **명령**: 삭제·이동 없이 **표기만**: 응암교회 관련 최상위 페이지 2-3곳 제목 앞에 `[아카이브·前사역지] ` 접두 부여(Notion update-page). 2024 수집 연구 템플릿 모음도 동일. 이동·통합은 §정리 후보로.
- **검증**: Notion 검색 "사역"에서 접두 표기가 결과에 보이는지 확인.
- **롤백**: 제목 원복(변경 목록을 07에 기록해 둠).

### FIX-15 — 스킬 원본 수집·버전관리 편입
- **대상**: F-15 (F-10의 선행) · **태그**: `[IMAC-ONLY]`
- **명령**:
  1. 탐색(읽기 전용): `ls ~/.claude/skills/ 2>/dev/null`, `find ~/Library/CloudStorage/Dropbox -maxdepth 3 -iname "*skill*" -type d 2>/dev/null`, Cowork 작업폴더(Claude Cowork GD 로컬 동기화 지점) 내 skill 폴더 검색. 발견 위치를 07에 기록.
  2. 수집: 발견된 12종(+dispatch-inbox 존재 시 함께) 원본을 repo `skills/<스킬명>/`로 **복사**(이동 아님).
  3. 대조: claude.ai 설명문(ListSkills)과 수집본 설명 필드 diff — 불일치는 07에 기록(어느 쪽이 최신인지 사용자 확인).
  4. 미발견 스킬은 claude.ai 웹에서 본문 복사→파일화 절차를 사용자에게 안내(`[USER-MANUAL]`).
- **검증**: `skills/` 아래 스킬 수 ≥ 12, 각 폴더에 SKILL.md 존재, 커밋 완료.
- **롤백**: 불필요(원본 무변경, 복사만).

---

## B. 위임 검증 명령 (V 시리즈) — 원격에서 확인 불가했던 영역

### V-01 `[IMAC-ONLY]` 엔진 실물 확인 (FIX-01 선행)
```bash
launchctl list | grep -iE "claude|dispatch|aicompany" ; crontab -l 2>/dev/null
ps aux | grep -iE "claude" | grep -v grep
ls ~/Library/LaunchAgents/ | grep -iE "claude|dispatch|aicompany"
```
+ 상주 Cowork/Code 세션의 고정 지시문(프로젝트 지침·첫 메시지) 사본 확보. 결과 전부 07에 붙여넣기.

### V-02 `[IMAC-ONLY]` 낡은 사역지 표기 잔존 검색
```bash
grep -rl "응암" ~/.claude ~/Library/CloudStorage/Dropbox/"Claude Dropbox" 2>/dev/null | head -20
```
발견 파일 목록을 07에 기록(수정은 사용자 확인 후 — 과거 기록물은 아카이브 가치가 있어 일괄 치환 금지).

### V-03 `[USER-MANUAL]` PAT 권한 최소화 확인
GitHub Settings→Developer settings→토큰의 스코프가 gist(+필요 시 repo)만인지 확인. 과권한이면 재발급·교체(gist-config.md 갱신).

### V-04 `[IMAC-ONLY]` Gist 데이터 실물 점검 (FIX-04·05 선행)
브라우저 또는 curl로 Gist 3종 raw JSON 확보(주소는 gist-config.md 참조) → ① `"Z"` 타임스탬프 잔존 건수 ② 실명·연락처 등 개인정보 포함 여부 ③ requests[]에 적체된 미처리 항목 ④ worklog의 실패 기록 → 4가지를 07에 요약.

### V-05 `[IMAC-ONLY]` 예약 트리거 확인
Cowork/Code 세션에서 Routine(트리거) 목록 조회 — 정기 회차가 Routine인지 로컬 cron인지 확정(FIX-01 문서에 기입).

---

## C. 정리 후보 (실행 금지 — 사용자 승인 대기 목록)

| 항목 | 사유 | 승인 전 상태 |
|---|---|---|
| 구 Gist 3종 (FIX-05 로테이션 실행 시) | 새 ID로 대체됨 | 무손상 보존 |
| Notion 응암교회·2024 연구 템플릿 페이지들 | 아카이브 섹션으로 이동 후보 | 접두 표기만(FIX-13) |
| notebooklm-reports 쌍둥이 중 래퍼화 이전 본문 | _core 통합으로 대체 | repo 이력에 보존 |
| 각 FIX의 `.bak` 백업 파일들 | 검증 완료 후 정리 대상 | 전부 보존 |
| dispatch-inbox 스킬(발견 시) | 현행 코드 미사용 — 용도 확인 필요 | 보존·용도 조사 |

> 이 목록은 빌드 종료 시점에 사용자에게 일괄 제시하고, **항목별 명시 승인**을 받은 것만 정리한다.
